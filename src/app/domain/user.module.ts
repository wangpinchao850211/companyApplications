export interface User {
  id?: string;
  email: string;
  name?: string;
  password?: string;
  avatar?: string;
  // projectIds?: string[];
  // taskIds?: string[];
  // address?: Address;
  // dateOfBirth?: string;
  // identity?: Identity;
}
